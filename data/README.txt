PIXORA PERSISTENT DATA
======================
Do not replace/delete this directory during code updates when using local-file storage.
The server writes data/accounts.json here automatically.
For cloud hosting/redeploy, DATABASE_URL (PostgreSQL) is strongly recommended.
